/**
 * @date 2019.01.11
 * @author 
 * @desc 비디오 이미지 실시간 설정
 * @param
 *  - obj_id : 객체 id
 *  - src_url : 실시간 url 정보 / ex. www.test.com
 * @return
 *
 */
function fnInitVideo(obj_id, src_url)
{
	alert("fnInitVideo() 함수 호출");
	/**
	 * 알고리즘 구현부분
	 */
}

/**
 * @date 2019.01.11
 * @author 
 * @desc 비디오 이미지 실시간 설정
 * @param
 *  - obj_id : 객체 id
 *  - colr_info : RGBA 값(#ff123311)
 *  - rect_info : 사각형 정보
 *   ex. rect_info = {"label":"man", "x": "10", "y": "20", "width" : "30", "height" : "40"}
 * @return
 *
 */
function fnDrawRect(obj_id, colr_info, rect_info)
{
	alert("fnDrawRect() 함수 호출");
	/**
	 * 알고리즘 구현부분
	 */
	alert(rect_info.y);
}